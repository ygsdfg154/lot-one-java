spring:
  application:
    name: lot-one-iot-worker
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        namespace: lot-one-dev
      config:
        server-addr: localhost:8848
        namespace: lot-one-dev
        file-extension: yml
        refresh-enabled: true
  profiles:
    active: dev
